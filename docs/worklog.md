# 작업 기록 (worklog)

## 2026-07-13 (6) — 실연동 준비 (서버 뼈대)
- **완료:**
  - APK 클라우드 빌드 성공 확인 (GitHub Actions run #1, 테스트 포함 6분) — Actions 탭 → Artifacts → solo-food-apk
  - `supabase/schema.sql`: profiles·fridge_items 테이블 + RLS(유저별 격리)
  - `supabase/functions/parse-receipt/index.ts`: 비전 LLM 영수증 파싱 Edge Function (구조화 출력, 앱의 MockReceiptParser와 동일한 응답 형태)
- **다음 할 일 (유저 계정 생성 후):**
  1. supabase.com 프로젝트 생성 → SQL Editor에 schema.sql 실행
  2. `supabase functions deploy parse-receipt` + `supabase secrets set ANTHROPIC_API_KEY=...`
  3. 앱: supabase_flutter 추가, S0 로그인, LocalStorage→Supabase 동기화, MockReceiptParser→HTTP 호출 교체
- **주의:** LLM 모델은 우선 claude-opus-4-8로 두었고, 비용 최적화가 필요하면 claude-haiku-4-5로 A/B 예정 (함수 상단 MODEL 상수)

## 2026-07-13 (5) — 로컬 영속화 + 유통기한 날짜화
- **목표:** 앱을 껐다 켜도 데이터 유지 (Supabase 전 단계 다리)
- **완료:**
  - `LocalStorage`(shared_preferences): 냉장고·냉파/버림 카운트·프로필 JSON 저장, 스토어 변경 시마다 자동 커밋
  - **유통기한을 고정 D-day에서 만료 날짜(expiresOn)로 리팩토링** — 시간이 지나면 신호등이 실제로 빨개진다 (영속화 시 필수 수정)
  - 검증: 테스트 16건 통과, 웹에서 온보딩 완료 → 새로고침 → 홈 유지 육안 확인
- **트러블슈팅 기록 (다음 세션 참고):**
  - `flutter pub add` 후 웹 빌드에 새 플러그인이 등록 안 되면(MissingPluginException) → `flutter clean` 후 재빌드
  - **웹에서 main()을 async로 만들면 첫 프레임이 붙지 않음** → 비동기 초기화는 `BootstrapApp`(FutureBuilder) 안에서 한다
  - 웹 미리보기 첫 렌더는 10초 이상 걸릴 수 있음 — 스크린샷 자동화는 15초 대기

## 2026-07-13 (4) — 온보딩·필터·보정·설정 → 1차 MVP 화면 전부 완성
- **목표:** 개발 순서 ④ 온보딩+하드 제약 필터, ⑤ B2 보정·S8 설정
- **완료:**
  - `UserProfile`(알레르기 + 식단 유형, 비건 금지 재료 확장), `ProfileStore`
  - S1·S2 온보딩: 알레르기 칩(법정 표시 대상 10종 + 직접 추가, "없어요" 크게) → 식단 유형 원탭 → 홈. 설정에서 재진입하는 수정 모드 포함
  - `HardConstraintGuard`: **RecipeService 데코레이터로 구현한 3겹 필터의 최종 검사.** 어떤 레시피 소스(모의/LLM)든 화면에 닿기 전 금지 재료를 코드로 재검사
  - B2 재료 보정 바텀시트: 아이콘/리스트 길게 누르기 → [다 먹음(임박이면 냉파 성공)/반 남음/버림(버림 카운트 기록, 죄책감 UX 금지)]
  - S8 설정: 프로필 요약·수정 진입, 처리방침 자리
  - 검증: analyze 0건, 테스트 16건 통과, 웹 미리보기에서 온보딩→홈→B2 육안 확인
- **1차 MVP 화면(S1~S8, B1~B2) 전부 구현 완료.** S0 로그인만 Supabase 연동 시 추가
- **다음 할 일:** 실연동 단계 — ① Supabase 프로젝트 생성(유저 계정 필요) ② LLM API 키 ③ Edge Function으로 영수증 파싱·레시피 변형 교체 ④ 데이터 영속화. 이후 실기기(안드로이드) 빌드 확인
- **주의:** 프로필·냉장고 모두 아직 메모리 저장 (새로고침하면 초기화) — Supabase 연동에서 해결

## 2026-07-13 (3) — 소비 루프
- **목표:** 개발 순서 ③ 소비 루프 (S6 → S7 → B1 차감 → 냉파 성공)
- **완료:**
  - `Recipe`/`RecipeIngredient` 모델, `RecipeService` 인터페이스 + `MockRecipeService`(내장 레시피 5개, 빨강 소진 기여도 랭킹 — 랭킹 규칙은 실서비스와 동일, 레시피 소스만 모의)
  - S6 파먹기 레시피 목록: "○○ 구출!" 임박 재료 칩, 커버리지 표시
  - S7 레시피 상세: 임박 재료 배너, 있는/없는 재료 구분, 큰 글씨 조리 단계, 유튜브 검색 딥링크(`url_launcher`)
  - B1 차감 바텀시트: 재료별 [안 씀/반/다 씀] 선택(레시피 기본값 제안), 확정 시에만 차감
  - `FridgeStore.applyDeductions`: 비율/개수 차감, 소진 시 제거, 임박 재료 소진 시 냉파 카운트 증가
  - 검증: analyze 0건, 테스트 10건 통과, 웹 미리보기에서 루프 전체(추천→차감→냉파 성공 토스트→뱃지 갱신) 육안 확인
- **다음 할 일:** 개발 순서 ④ 온보딩(S0~S2) + 하드 제약 3겹 필터. 이후 ⑤ B2 보정·S8 설정 → 1차 MVP 화면 완성. 그다음 Supabase·LLM 실연동(유저 계정 필요)
- **주의:** 유튜브 딥링크는 실기기에서 확인 필요 (웹 미리보기에선 새 탭)

## 2026-07-13 (2) — 입력 루프
- **목표:** 개발 순서 ② 입력 루프 (S4 → S5 → 냉장고 반영)
- **완료:**
  - `FridgeStore` (ChangeNotifier) — 냉장고 상태를 앱 전역 공유. Supabase 연동 시 이 클래스 내부만 교체
  - `ReceiptParser` 인터페이스 + `MockReceiptParser` — 붙여넣은 텍스트에서 식재료 사전 매칭, 개수 추정("10구"), 비식재료 제외. **LLM 연동 전 개발용 모의 구현** (Edge Function으로 교체 예정)
  - S4 재료 채우기 화면: 영수증 찍기(모의) / 주문내역 붙여넣기 탭, 로딩 오버레이, 인식 실패 안내
  - S5 인식 결과 확인 화면: 개수 스테퍼·항목 삭제·제외 항목 열람, "냉장고에 담기" 확정 → S3 복귀
  - 검증: analyze 0건, 테스트 5건 통과(입력 루프 위젯 테스트 포함), 웹 미리보기에서 전체 흐름 육안 확인
- **다음 할 일:** 개발 순서 ③ 소비 루프 (S6 파먹기 레시피 목록 → S7 상세 → B1 차감). 그 후 Supabase·LLM 실연동 (유저 계정 생성 필요 — 안내 예정)
- **주의:** `MockReceiptParser`는 실서비스 코드가 아님. 실연동 시 `main.dart`의 주입부만 교체

## 2026-07-13
- **목표:** 기획 완성 + Flutter 프로젝트 셋업 + S3(비주얼 냉장고) 골격
- **완료:**
  - `idea.md` 기획 완성 (킬러 기능 5개, 레시피 엔진 아키텍처, 타겟·획득 전략, 수치화된 MVP 로드맵, 기술 스택 확정)
  - `docs/screens.md` 1차 MVP 화면 흐름 설계 (화면 7개 + 바텀시트 2개)
  - Flutter 3.44.6 프로젝트 생성 (`app/`, android+web 플랫폼)
  - S3 냉장고 골격 구현: 더미 데이터 10종, 냉장고 단면(선반 3칸+문짝+냉동), 신호등 테두리, 채움 게이지, 개수 뱃지, 아이콘↔리스트 토글, "빨간 애들 털어먹기" 버튼(스텁), 냉파 카운트 뱃지(더미)
  - 검증: `flutter analyze` 0건, `flutter test` 2건 통과, 웹 빌드 스크린샷으로 두 뷰 육안 확인
  - `CLAUDE.md` 7번 섹션(프로젝트 메모) 채움 — 실행·확인 방법 포함
- **다음 할 일:** 개발 순서 ② 입력 루프 (S4 영수증 촬영 → S5 파싱 확인 → 냉장고 반영). Supabase 프로젝트 생성 및 연결도 이 단계에서 필요.
- **주의:**
  - 웹 미리보기용 `app/web/flutter_bootstrap.js` 삭제 금지 (canvaskit 로컬 로드)
  - 웹 미리보기에서 이모지 ☒ 깨짐은 정상 (실기기에선 정상). 전용 아이콘 제작 전까지 이모지가 플레이스홀더
  - 재료 아이콘 자리는 이모지 → 추후 전용 아이콘 60종으로 교체 예정 (idea.md 킬러 기능 3)
